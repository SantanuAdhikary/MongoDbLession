
//! 1. Find all employees where the `hiredate` is stored as a date type.


//! 2. Find all employees where the `comm` field is stored as a number (int or double).


//! 3. Find all employees where the `ename` is stored as a string.





































// 1. db.emp.find({
//   hiredate: { $type: "date" }
// })
     
// 2. db.emp.find({
//        comm: { $type: "number" }
//      })
     
// 3. db.emp.find({
//        ename: { $type: "string" }
//      })
     

